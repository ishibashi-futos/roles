- [ ] `REQUIREMENT DEFINITION` / `DISCUSSION POINTS` / `ROLES` を JSON形式でクリップボードにコピーする機能の追加
- [ ] `REQUIREMENT DEFINITION` / `DISCUSSION POINTS` / `ROLES` を Pasete してインポートできる機能を追加
- [x] 会話履歴を 下に入れていくのではなく、対話ログの上に挿入・テーマを入力したらテーマ入力のboxでそのまま `追加回答` も入力できるように
- [ ] サーバが再起動したら、クライアントも自動的にリロードする機能の追加
- [x] 議論する言語を指定できる。起動する環境変数で指定する。 "en" | "ja", default = "ja"
- [x] Lobbyで、送信ボックスやボタンが無効なタイミング（要件定義役の処理中など）に、ボタンなどが非活性に見えないのでわかりやすくする
- [x] index page `/` を、新規セッションの開始ボタン + 過去セッションの一覧を出す画面にしてほしい
- [x] 過去セッションの削除ボタンを追加する
- [x] アイコンを押すと `/` に戻れる
- [ ] 同時に実行できるのは１セッションだけに絞る。非同期実行はさせず、セッション一覧で pending として出せるようにする
- [x] ロール自動生成時にCxO等上位職種を含めたり、任意の役割を誘導できるよう、要件定義エージェントのシステムプロンプトを補正する
- [x] CLIでの実行モードを追加する
- [ ] CLI modeで `--wait` / 非 `--wait` の時、現在何をしているか？作業状況を標準出力に出すようにする。
