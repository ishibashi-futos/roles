{
  "name": "roles",
  "bin": {
    "roles": "./src/index.ts"
  },
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "format": "biome format --write .",
    "format:check": "biome check .",
    "typecheck": "tsc --noEmit ",
    "sanity": "bun run format && bun run typecheck && bun test",
    "dev": "bun run --watch src/index.ts serve",
    "dev:en": "ROLES_OUTPUT_LANGUAGE='en' bun run --watch src/index.ts serve"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.6",
    "@tailwindcss/cli": "^4.2.1",
    "@types/bun": "latest",
    "tailwindcss": "^4.2.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "hono": "^4.12.7"
  }
}
